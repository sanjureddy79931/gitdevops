apiVersion: batch/v1
kind: Job

metadata:
  name: hello-python

spec:
  

  template:
  
    spec:
      containers:
      - name: hello-python
        image: hello-python:latest
        imagePullPolicy: Never
      restartPolicy: Never  
